version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - REPLICATE_AI_KEY=${REPLICATE_AI_KEY}
      - DATA=/data
    volumes:
      - ./data:/data
    restart: unless-stopped
